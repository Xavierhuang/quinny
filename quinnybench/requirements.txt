pytest>=8.0

# Provider SDKs — install only the ones you have keys for.
anthropic>=0.40.0
openai>=1.50.0
google-genai>=0.3.0

# xAI / DeepSeek / OpenRouter all use the openai SDK above (compatible protocol).
